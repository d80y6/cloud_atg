<?php
class accessControl {
  public function accessCtrl()
  {
      // load the instance
	  $this->CI =& get_instance();
	  
	//   $pword = $this->CI->session->userdata('password');

	// echo $this->CI->uri->segment(1);s
		
	// 	if ($this->CI->uri->segment(1) != 'logout' ){
	// 		if  ($this->CI->session->userdata('id') == null ){
	// 				redirect('logout');

	// 		}
	// //   redirect('logout');
	// 		// echo "Niiiiiiiiiiiiiiiiiiiiiiiiiice";
			
		// }	

	
  }
}
